{
  "name": "hookli-monorepo",
  "private": true,
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev",
    "gen:manifest": "pnpm --filter hookli run gen:manifest"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
