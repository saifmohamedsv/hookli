<svg width="1200" height="400" viewBox="0 0 1200 400" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="use(hookli) — Simple React hooks. Typed. SSR-safe. Zero dependencies.">
  <!-- solid ground, no gradient -->
  <rect width="1200" height="400" fill="#0B1120"/>

  <!-- wordmark: a React use() call -->
  <text x="600" y="212" text-anchor="middle"
        font-family="ui-monospace, 'SF Mono', 'JetBrains Mono', Menlo, monospace"
        font-size="104" font-weight="700" letter-spacing="-2">
    <tspan fill="#475569">use(</tspan><tspan fill="#61DAFB">hookli</tspan><tspan fill="#475569">)</tspan>
  </text>

  <!-- terminal cursor -->
  <rect x="952" y="130" width="14" height="88" rx="2" fill="#61DAFB"/>

  <!-- tagline -->
  <text x="600" y="278" text-anchor="middle"
        font-family="ui-monospace, 'SF Mono', Menlo, monospace"
        font-size="27" font-weight="400" fill="#94A3B8">
    Simple React hooks. Typed. SSR-safe. Zero dependencies.
  </text>
</svg>
