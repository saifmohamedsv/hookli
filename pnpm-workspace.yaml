packages:
  - "packages/*"
  - "apps/*"

# pnpm 11: don't hard-fail scripts on the pre-run deps check (esbuild/sharp ship
# prebuilt binaries via optional deps, so their postinstall scripts aren't needed).
verifyDepsBeforeRun: false

onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
